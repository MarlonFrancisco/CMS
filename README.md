# cms created for easy content creation

# Comandos

- npm run dev :: started server for development
- npm run build:dev  :: build project files in mode development
- npm run build:prod :: build project file in mode production